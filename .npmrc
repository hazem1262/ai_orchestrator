# pnpm defaults are fine here. NOTE: this file previously set auto-install-peers=false alongside a
# @wakecap registry line whose ${GITHUB_PACKAGES_TOKEN} never resolved — pnpm then skipped the whole
# file, so that setting never actually applied and the tree was built with peers auto-installed.
# Turning it on for real breaks peer resolution (14 typecheck errors, 49 tests lost), so it stays off.
